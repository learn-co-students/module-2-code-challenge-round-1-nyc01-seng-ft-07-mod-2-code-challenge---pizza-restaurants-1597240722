
<% if @restaurant_pizza.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@restaurant_pizza.errors.count, "error") %> prohibited this article from being saved:</h2>
 
    <ul>
    <% @restaurant_pizza.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>






<%= form_for @restaurant_pizza do |f| %>
<%= f.label :pizza %>
<%= f.collection_select :pizza_id, Pizza.all, :id, :name %>

<%= f.label :restaurant %>
<%= f.collection_select :restaurant_id, Restaurant.all, :id, :name %>

<%= f.label :price %>
<%= f.number_field :price, in: 1..30 %>
<%= f.submit %>
<% end %>
